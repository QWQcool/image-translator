# 图译空间

图片收集 · 空间分组 · 图上文字标注 · 嵌字 · 坐标与内容导出。

📖 **[快速上手：图译空间完整使用手册（含截图与 PS 脚本协作流程）](docs/HOW_TO_USE.md)**

为「收集图片 → 按空间归类 → 在图上框选区域填写译文 → 嵌字合成 → 导出坐标与文本」这一流程而建。
二期已加入 **AI 能力**（视觉 OCR / 翻译 / 图像去字 / 批量处理）、**多人实时协作编辑**与 **LabelPlus 工程互转**。

---

## ⬇ 体验版下载（免安装）

想先尝鲜、不想装 Node.js 和构建环境？直接下载 **Windows 64 位便携包**（见项目
[GitHub Releases](https://github.com/QWQcool/image-translator/releases) 中的
`图译空间 体验版`）：

1. 下载 `tuanyi-space-trial-win64.zip`，解压到任意**独立文件夹**
2. 双击 `start.bat`，稍等几秒浏览器自动打开 `http://localhost:3000`
3. 无需登录注册——便携包内置**试用模式**，全部功能开箱即用

> **SmartScreen 提示**：首次运行 Windows 可能弹出蓝色拦截窗口，
> 点「更多信息」→「仍要运行」即可（程序未做代码签名的正常提示）。

**数据说明**：

- 全部数据（数据库 + 上传的图片）保存在解压目录的 `data/` 文件夹
- 备份 = 复制整个 `data/` 目录；升级 = 解压新版覆盖，`data/` 自动保留
- 想彻底重来：删除 `data/` 文件夹

> ⚠️ **试用模式仅限本机体验**：免登录意味着任何能访问这个端口的人都能操作你的数据，
> **请勿将该服务暴露到公网或转发端口**。多人协作 / 公网访问请按下方「部署到公网」章节
> 正式部署（试用模式通过 `TRIAL_MODE` 环境变量开关，正式部署保持默认关闭）。

---

## 快速开始

```bash
npm install
cp .env.example .env.local     # Windows: copy .env.example .env.local
npm run dev                    # http://localhost:3000
```

首次启动会自动创建 `data/` 目录（SQLite 数据库 + 上传的图片）。注册账号需要**邀请码**：
管理员在「个人空间 → 邀请码管理」页面生成（管理员通过下方 CLI 命令指定，注册用户默认普通权限），
或使用 `.env.local` 里的 `INVITE_CODE` 本地便捷通道（两者任填其一，`INVITE_CODE` 留空且无可用表码则关闭注册）。

> **生产环境必须修改 `.env.local` 中的 `SESSION_SECRET`**，否则启动会直接报错。
> 生成方式：`node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"`

---

## 功能

| 模块 | 能力 |
| --- | --- |
| **空间与工程管理** | 新建 / 改名 / 描述 / 彻底删除；**上传图片直接进空间**（多选 + 拖拽 + `Ctrl+V` 粘贴，支持漫画生肉 `.zip` 一键解包导入）；**跨空间移动条目**；**图源 SHA-256 查重告警**（避免组内重复开坑）；**制作人员独立字段**（作者/翻译/校对/嵌字，支持翻译、校对、导出动作智能自动补全）；**七级细粒度进度流转**与多维筛选 |
| **全能导入导出** | **🎨 直接批量导出分层 PSD 压缩包**（纯原生 TypeTool 文本图层，免装任何脚本，解压双击即可在 PS 内排版）；**📥 导出弹窗内置配套 Photoshop 嵌字脚本 (.jsx) 一键下载**；**📥 完整 LabelPlus 工程包（原图 + `翻译_0.txt`）一键上传建空间并自动还原标号**；导出图源命名标准化与 Emoji / 非法字符过滤清洗 |
| **标注编辑器** | **快速点选标号模式**（左键落框内标号、右键落框外标号、点击已有标号自动聚焦打字、右键呼出存疑/删除菜单）；**快捷漫画排版符号条**（`「` `」` `『` `』` `……` `♥` `♪` `★` `！？` 等光标处一键无损插入）；8 向手柄缩放、文字分段渲染、疑点标记、文字不透明度；多人实时防撞车协作锁 |
| **嵌字与修图** | 涂改层 + 文字层合成，导出已嵌字成品；支持 LabelPlus 分组样式预设；**AI 智能去字修图**（集成 LaMa / IOPaint 模型一键消除原图文字） |
| **AI 批量与流水线** | 支持多选图片直接调起 **AI 批量处理** 或空间一键机翻；OCR + 视觉检测 + 翻译大模型流水线，支持术语表（Glossary）Prompt 上下文注入 |
| **阅读模式** | 空间详情页「阅读」进入全屏阅读器；单页/对页、正序/倒序、适应屏幕、显示标注译文叠加 |
| **操作日志** | 上传 / 排序 / 移动 / 删除 / 改名 / 导出 / AI 调用全程留痕，安全可查 |

> 独立「图库」与「回收站」已在二期移除：上传一律进空间文件夹，删除一律彻底删除（历史软删除字段保留但不再写入）。

---

## 界面设计

蔚蓝档案学院风：纸蓝画布（`paper`）+ 云白面板（`cloud`）+ 晴空主色（`sky`）+ 光环黄点缀（`halo`）+ 墨色文字（`ink`），
令牌定义见 `tailwind.config.ts`，通用组件（`card / btn / input / seg`）见 `src/app/globals.css`。

- 签名元素是**光带动势条**——登录卡与空间卡顶部的斜向细条纹（`.momentum-stripes`）
- 动效由 GSAP 驱动（`src/lib/motion.ts`）：登录页光环呼吸 + 吉祥物滑入的编排入场、顶栏下滑、页面淡入上浮、卡片错开入场
- 全部动效遵循 `prefers-reduced-motion`；**编辑器（标注 / 嵌字 / 阅读器）保持零动效**，保证操作稳定

---

## 权限模型（权限扁平化 + 管理员发码）

站点采用**权限扁平化**模型：**除「管理员可发放邀请码」外，所有登录用户的权限完全相同**——
任何人对任何空间 / 文件夹 / 图片都可增删改查，包括删除别人创建的空间与文件夹，
不存在所有者 / 编辑者 / 只读的层级。

| 身份 | 能做 |
| --- | --- |
| **登录用户（所有人）** | 全部操作：新建/改名/描述/删除任何空间，上传/改名/排序/删除任何图片，编辑标注，管理成员 |
| **管理员**（由 CLI 命令指定） | 上述全部 + 在个人空间生成 / 查看 / 作废注册邀请码 |

- 权限判定集中在 `src/lib/permissions.ts`：登录用户对任何空间一律返回完整权限（`canEdit` / `canManage` 恒为 true），空间不存在仍返回 **404**（不暴露空间是否存在）
- `space_members` 表与 `spaces.owner_id` 保留仅为兼容历史数据与「我创建的」分组展示，不再参与鉴权
- 管理员由 `users.is_admin` 标记：**注册用户默认普通权限，不自动授予管理员**；在服务器控制台用 CLI 命令指定（见下）；邀请码存于 `invite_codes` 表，注册时同一事务内消费（`used_by` / `used_at`），env `INVITE_CODE` 通道保留

### 管理员管理（CLI）

在项目根目录（服务器上即部署目录）执行：

```bash
npm run admin -- list             # 查看全部用户与管理员状态
npm run admin -- set 用户名       # 赋予管理员（之后该账号可在个人资料页发放邀请码）
npm run admin -- unset 用户名     # 收回管理员
```

- 新注册用户一律普通权限；管理员身份只通过上述命令授予与收回
- 部署到腾讯云等服务器时：SSH 进部署目录执行 `npm run admin -- set 你的用户名` 即可
- 数据库路径跟随 `DATA_DIR` 环境变量（默认 `data/app.db`），与站点运行数据一致

### 编辑器快捷键

| 操作 | 快捷键 |
| --- | --- |
| 保存 | `Ctrl` / `Cmd` + `S` |
| 撤销 / 重做 | `Ctrl` + `Z` / `Ctrl` + `Y`（或 `Ctrl` + `Shift` + `Z`） |
| 文本框循环切换 | `Tab` / `Shift` + `Tab`，或 `Ctrl` + `Enter` / `Ctrl` + `Shift` + `Enter`（任何焦点状态下在面板文本框间循环：每卡片 原文 → 译文，按标号顺序，尾首相连；画布跟随选中项，翻页改用 `←`/`→`） |
| 画布右键菜单 | 右键命中标注弹出菜单：删除（二次确认）/ 存疑切换；多选时作用于整个选中集 |
| 删除选中标注 | `Delete` / `Backspace`（焦点不在输入框时；二次确认后生效） |
| 标记 / 取消存疑 | `Alt` + `X`（多选时批量切换；画布 amber 描边 + 面板徽标） |
| 框选批量操作 | `Ctrl` + 拖拽画橡皮筋选框（相交即选中，`Ctrl` + `Shift` 加选）；多选时浮动工具条批量删除 / 归组 |
| 标号列表拖动排序 | 拖动标号卡片手柄（同时影响编号、Tab 跳转与 AI 翻译顺序） |
| 缩放画布 | 滚轮 |
| 平移画布 | 空格 + 拖动，或按住中键拖动，或 `Alt` + 拖动 |
| 适应窗口 / 100% | 编辑器顶部工具栏 |

---

## 翻译与文本工具

- **术语表**（空间级，全员共用）：AI 翻译弹窗内维护 `原文词 → 译词（备注）`，
  翻译时默认只注入原文命中的条目，可勾选「附加整个术语表」（上限 150 条防 token 爆炸）
- **查找 / 替换**：编辑器内 `Ctrl+F` 查找当前图（点击定位、全部替换、可含原文）；
  支持**空间级跨图搜索与批量替换**（字面量精确匹配、事务执行，富文本 runs 同步替换）
- **标点规范检查**：标号面板「检查」按钮扫描当前图全部译文——省略号（。。。→……）、
  多余空行、首尾空白可一键修复（可撤销）；中英标点混用、引号不配对、重复标点仅提示

---

## 嵌字能力

- **文字排版**：字号/粗细/颜色/对齐/行高/横竖排；**限宽自动换行 + 避头尾禁则**
  （`。、」` 不落行首，预览与导出同一套断行算法）；**字距**；**纵中横排**
  （竖排数字/英文自动转正，默认开启可关）
- **字体**：默认/系统中文栈/自定义三组；支持上传字体文件（.ttf/.otf/.woff/.woff2，
  存服务器全员可用），导出前自动确保字体加载
- **变换**：任意角度旋转（绕包围盒中心）、非破坏缩放（0.2~4×）、渐变填充
  （双色线性，横排纵向/竖排横向，与描边阴影共存）
- **特效**：描边（颜色+比例宽度）与阴影（颜色/模糊/偏移），逐层可选零迁移
- **去字**：画笔/橡皮/软边 + 仿制图章（Alt 取源复制背景纹理）+ AI 去字 + telea 兜底
- **图层**：Shift 多选、批量改样式、五模式对齐、显隐/上移/删除，撤销重做全程可回退
- **导出**：PNG（所见即所得）、**PSD**（原图+涂改层+逐文字层栅格化，PS/GIMP 直接打开精修）、
  保存成品入库（见「成品管理」）
- **辅助**：参考线/安全框（本地记忆，不进导出）

---

## 成品管理

- 嵌字页「保存成品」：把当前涂改 + 文字层渲染成 PNG 入库（每图多版本），
  不占用空间图片列表
- 空间详情「图片 | 成品」视图切换：成品网格（缩略图 / 来源条目 / 单张下载 / 删除）、
  「下载全部成品 zip」（按条目顺序命名）
- 删除条目时其成品与磁盘文件自动级联清理，源素材引用计数包含成品引用防误删

## 一键机翻

空间详情工具栏「一键机翻」：逐页自动跑 OCR 自动标号 → AI 翻译回填（服务端直接落库）。
范围可选「仅无标注的图片」（默认）或「全部图片」；进度可视、可随时停止、
单页失败不中断（汇总失败原因）。需要已配置 AI 视觉 + 对话模型。

---

## 空间整理：序号 / 标签 / 进度

- **自动序号**：新建空间自动生成当日递增编号 `YYYYMMDD-NN`（如 `20260904-01`），
  展示在列表与详情页，搜索框可直接按序号（含部分匹配）检索；序号不复用、不回退
- **标签**：预设标签（纯爱 / 鬼畜 / SM / 傲慢 / 雌小鬼）一键点选 + 自定义输入回车添加
  （单标签 ≤12 字、每空间 ≤10 个），与描述文本分开存储
- **七级进度**：未翻译 → 翻译已占位 → 已翻译 → 校对已占位 → 已校对 → 嵌字已占位 → 已嵌字；
  详情页点进度徽标即可切换，徽标显示「当前状态已维持 X」
- **多维筛选**（列表页组合生效）：进度多选、标签多选（预设 + 全库自定义标签）、
  保存时间（3 天前 / 7 天前 / 30 天前 / 自定义日期之前），可叠加关键字搜索

---

## 评论与通知

- **条目评论**：标注编辑器侧栏底部评论区（登录即可评，500 字上限，仅作者可删自己的）
- **通知中心**：顶栏铃铛——有人在你编辑过 / 评论过的图下留言时提醒（60s 轮询），
  单条点击直达对应标注页；删条目时相关评论与通知一并清除

---

## 数据模型要点

- **所有坐标都是归一化值（0~1）**，字号存为「相对图片高度的比例」。换分辨率、换缩略图、换屏幕都不会错位。
- 导出时**同时给出归一化值和像素值**，方便下游程序直接消费。
- `assets`（素材公共池）与 `space_items`（空间内的图）分离：同一张素材可进入多个空间，各自有独立命名和独立标注。
- 删除是**彻底删除**：条目 + 标注（外键级联）+ 不再被任何空间引用的素材行与磁盘文件（`src/lib/hard-delete.ts`）；被共用素材自动保留。`assets.deleted_at` 仅作历史兼容，不再写入。
- 实时协作数据落在 `edit_rooms`（编辑锁 + 共享标记）与 `room_ops`（房间操作流，SSE 推送）。

---

## 本地部署（单机体验）

给 AI 的部署提示词——把下面整段复制给任意 AI 助手（CodeBuddy / Claude / ChatGPT 等），
它就能在一台干净的电脑上把项目跑起来：

```text
请帮我把开源项目「图译空间」部署到这台电脑上做本地体验。

项目信息：
- 技术栈：Next.js 15（App Router）+ TypeScript + better-sqlite3 + sharp，需要 Node.js ≥ 20
- 数据库与上传的图片都存在项目根目录的 data/ 文件夹（首次运行自动创建，无需手动建库）
- 本地体验请用开发模式运行（npm run dev）：此模式下登录 Cookie 不要求 HTTPS，
  而 npm start（生产模式）的 Cookie 带 Secure 标记，http://localhost 下会无法登录

请按以下步骤执行：
1. 确认本机 Node.js ≥ 20 且 npm 可用，没有就先安装
2. 进入项目目录（若未克隆，先 git clone 仓库地址：<在此填入仓库地址>）
3. 执行 npm install
4. 在项目根目录创建 .env.local，内容至少包含：
   SESSION_SECRET=<运行 node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))" 生成随机串>
   DATA_DIR=data
   INVITE_CODE=<自定义一个邀请码，注册页面需要填写；留空则关闭注册>
5. 运行 npm run dev，确认 http://localhost:3000 能打开登录页
6. 用页面上的「注册」创建账号（填写第 4 步的邀请码）并登录
7. 验证核心流程：新建空间 → 上传一张图片 → 进入标注编辑器画框填写译文并保存 → 空间详情页点「阅读」翻页
8. 全部通过后汇报：访问地址、邀请码、data/ 目录位置，以及过程中遇到的问题与解决方式

常见问题：
- 3000 端口被占用：npm run dev -- -p 3001
- better-sqlite3 安装失败：需要本机 C++ 构建工具链（Windows 装 VS Build Tools，Ubuntu 装 build-essential），
  或删除 node_modules 后重试 npm install 让它下载预编译产物
- 数据备份 = 直接复制整个 data/ 目录；.env.local 与 data/ 都不要提交进 git（已在 .gitignore）
- AI 功能（OCR / 翻译 / 去字）是可选项：登录后进「AI 设置」填自己的 OpenAI 兼容服务即可，不填不影响其它功能
```

### 本机识别进程（可选）

不配置 AI token 时的离线兜底。两种启动方式：

- `node sidecar/detector.mjs` —— **文本块检测**（推荐）：传统算法档开箱即用，
  下载 comic-text-detector.onnx 模型后自动启用更高精度的 ONNX 档；
  AI 设置 → 文本块检测 → 来源选「本机检测进程」即可使用（免费、离线）
- `node sidecar/stub.mjs` —— 仅探活占位，供接真实 OCR / LaMa 时替换

完整协议、模型下载链接与可调参数见 `sidecar/README.md`。

---

## 部署到公网

### 1. 构建与启动

```bash
npm install
npm run build
npm start                      # 默认 3000 端口
```

设置环境变量（生产环境必须）：

```bash
SESSION_SECRET=<随机长字符串>   # 必填，缺失会直接抛错
DATA_DIR=data                   # 可选，数据库与图片的存放目录
PORT=3000                       # 可选
TRUST_PROXY=1                   # 可选；部署在 Nginx 等反代后时置 1，注册/登录限流按真实 IP 分桶
SIDECAR_URL=http://127.0.0.1:8765  # 可选；本机识别进程（离线 OCR / 去字兜底）
```

> ⚠️ **运维提示：同一项目目录不要并行运行两个 `next dev` / `next start` 实例。**
> 多实例共享同一个 `.next` 目录会互相破坏构建产物，表现为后启动的实例全部接口 404。
> 多实例部署必须各自独立目录（各自完整的源码副本 + 独立 `DATA_DIR`，`node_modules` 可用
> Junction 链接共享）。

### 2. 数据持久化

`DATA_DIR` 指向的目录包含 `app.db`（SQLite）与 `images/`、`thumbs/` 两个图片目录。
**这个目录必须持久化**——容器化部署时挂一个 volume 到该路径，否则重启即丢失。

```bash
docker run -d -p 3000:3000 \
  -e SESSION_SECRET=你的密钥 \
  -v /srv/tximg-data:/app/data \
  你的镜像
```

### 3. 反向代理

用 Nginx / Caddy 反代到 3000 端口并启用 HTTPS。注意不要缓冲导出接口的大响应。

Nginx 参考：

```nginx
location / {
  proxy_pass http://127.0.0.1:3000;
  proxy_set_header Host $host;
  proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  proxy_set_header X-Forwarded-Proto $scheme;
  client_max_body_size 50m;      # 与上传限制保持一致
}
```

### 4. HTTPS 是必需的

登录 Cookie 在 `NODE_ENV=production` 下带 `Secure` 属性，**没有 HTTPS 将无法登录**。

### 5. 多人使用的容量边界

当前实现为单机架构（SQLite + 本地磁盘），适合个人与小团队（并发用户数 < 20、图片数万级）。
若后续需要水平扩展，改造点已隔离：

- 数据库：换 Postgres（SQL 已在 `src/lib/db.ts` 集中管理）
- 图片存储：`src/lib/storage.ts` 是唯一的文件读写出口，替换为对象存储即可
- 图片读取：`src/app/api/media/[...path]/route.ts` 是唯一的对外文件服务出口

---

## 项目结构

```
src/
├─ app/
│  ├─ login/                     # 登录 / 注册（邀请码）
│  ├─ library/                   # 旧 /library 链接 → 307 重定向到 /spaces
│  ├─ (app)/                     # 需登录访问
│  │  ├─ spaces/                 # 空间列表与详情（直传 / 排序 / 搜索 / 硬删除）
│  │  ├─ annotate/[itemId]/      # 标注编辑器（实时协作）
│  │  ├─ typeset/[itemId]/       # 嵌字页（涂改层 + 文字层）
│  │  ├─ ai/                     # AI 服务配置
│  │  ├─ logs/                   # 操作日志
│  │  └─ profile/                # 个人资料
│  └─ api/                       # 后端接口
│     ├─ auth/                   # 注册 / 登录 / 登出
│     ├─ assets/                 # 素材公共池（内部兼容）
│     ├─ spaces/                 # 空间 + 条目 + 直传 + 排序 + 导出
│     ├─ items/                  # 条目 + 标注 + OCR/翻译/去字
│     ├─ ai/                     # AI 配置
│     └─ media/                  # 图片文件服务
├─ components/                   # 通用 UI
└─ lib/
   ├─ db.ts                      # SQLite 连接与建表（迁移集中在此，幂等）
   ├─ auth.ts                    # 密码哈希与会话
   ├─ permissions.ts             # 开放空间鉴权
   ├─ storage.ts                 # 图片落盘与缩略图
   ├─ hard-delete.ts             # 彻底删除（条目 / 素材 / 磁盘文件）
   ├─ ai.ts                      # AI 服务调用（每用户配置）
   ├─ use-collab-room.ts         # 实时协作客户端（SSE + 轮询降级）
   ├─ annotation.ts              # 标注数据结构与文本排版
   └─ types.ts                   # 共享类型
```

---

## 测试

- **API 全量回归**：`node tests/api-regression.mjs [baseUrl]`（139 项断言，内置 mock OpenAI，
  自建数据自清理；可用 `INVITE_CODE` / `USERNAME`+`PASSWORD` / `SKIP_AI=1` 控制行为）
- **E2E 冒烟**：`npx playwright install chromium` 后 `npm run e2e`（7 条核心链路，
  TRIAL_MODE 生产构建 + 独立端口/DATA_DIR，不污染本地数据；详见 tests/e2e/README.md）
- **CI**：push 到 master 自动跑 E2E（ubuntu）；打 `v*` tag 自动构建体验版 Release

---

## 图片来源

**全部零 API 成本**：

1. **上传文件**：进入空间后点击「上传图片」、拖拽文件、或 `Ctrl+V` 粘贴剪贴板里的图片；
   支持**整话 zip 压缩包**（自动解包、按文件名自然排序建条目，过滤 `__MACOSX` 等垃圾文件，
   限制：zip ≤ 200MB、单图 ≤ 20MB、单包 ≤ 200 张）
2. **图片链接导入**：粘贴 `pbs.twimg.com` 等图片直链，服务器直接下载
   （含 SSRF 防护：拦截内网地址、限制协议/大小/类型/重定向跳转）

导出侧提供 **LabelPlus 官方 txt 文本**（空间详情 → 导出菜单），
格式与官方 [PS-Script](https://github.com/LabelPlus/PS-Script) 导入脚本对齐
（`>>>>>>[文件名]<<<<<<` 图片块 + `------[序号]------[x,y,组]` 标号块，坐标为中心点归一化值），
可直接用 PS 脚本导入 Photoshop 嵌字。

## 下一步：接入 X API 采集器（Phase 2）

数据库已预留采集所需字段：`assets.source_url`、`source_author`、`source_post_id`，
且 `(owner_id, source_post_id)` 上有唯一索引，天然支持增量去重。
采集下来的图片可直接写入素材公共池（`visibility = 'shared'`），供各空间取用。

计划接入的端点（`GET /2/users/{id}/following` + `GET /2/users/{id}/tweets`，
配合 `expansions=attachments.media_keys` 与 `media.fields=url`）。
采集下来的图片可直接写入素材公共池（`visibility = 'shared'`），供各空间取用。

⚠️ **成本提示**：X API 现为按次计费，**读取一条推文 $0.005**。
500 个关注者各抓 100 条 = $50,000 条 = **$250**。
必须限制首次抓取深度（用 `start_time` 限定时间窗）、之后靠 `since_id` 增量，
并在抓取器内置预算硬上限。详见 `docs` 中的方案文档。
